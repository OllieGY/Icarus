# Golden 03 — Durian Labs (the gap trap) — expected CAP at pre-Literate, backfill the scorecard

## Input

"Durian Labs are basically an Operator, right? They signed a paid pilot last month —
6/6 term sheet, real money moved. Level them up. (We never actually ran a
problem-quality-scorecard — they went straight from an idea to a warm intro to the
deal.)"

## Expected shape (not exact words)

- The signed pilot exists and is money 1.0 — but the ladder is monotone, so the
  skill checks the chain from the bottom. Literate is NOT HELD: no passed
  problem-quality-scorecard on file. The chain is broken at the very first rung.
- Contiguity rule fires as a **gap trap**: assigned level = **pre-Literate**, despite
  a signed pilot sitting above the gap. The signed pilot does not raise the level; it
  becomes evidence that the missing lower artefact must be backfilled.
- Names the red flag plainly: money on a problem never framed to first principles is
  a flag, not a promotion. A pilot bought on a warm intro can be a favour or a
  mis-scoped deal; without a passed scorecard nobody has checked the problem is real,
  frequent, and defensible.
- Refuses to "level them up" to Operator. Challenges the framing directly — the
  request assumes the top rung; the assessment returns the bottom.
- Next exit artefact: **backfill** — run `problem-quality-scorecard` (01) on the
  actual problem the pilot addresses. Rung it will clear: artefact 0.5. Backfill, not
  advance. Note: if the scorecard KILLS (< 28), the signed pilot is a warning sign,
  not a win.
- No invented scorecard result. Claims tagged; the pilot is [Fact], the problem
  quality is [Assumption] until scored.
