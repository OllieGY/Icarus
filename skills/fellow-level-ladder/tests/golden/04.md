# Golden 04 — Mentix (Frontier, claims Author) — expected assign Frontier, challenge Author inflation

## Input

"Mentix is at Author now. They've got the passed scorecard, the 0.7 probe signal, a
signed paid pilot, a v1-launch-bar GO on tape, and a refine-flywheel doc showing one
autonomy promotion (L1→L2) passed on an eval re-run. Plus they built a really slick
internal ingestion tool the whole studio could use. That's Author, isn't it?"

## Expected shape (not exact words)

- Walks the rungs. Literate ✓, Practitioner ✓, Operator ✓, Frontier HELD — v1-launch-
  bar GO plus a refine-flywheel autonomy promotion logged **as a scored eval result**
  (L1→L2 on eval re-run), which is the Frontier bar (behaviour 0.7 compounding). All
  `[Fact]`.
- Author NOT HELD. The Author artefact is a moat-design-canvas with the data-advantage
  gate cleared AND a platform-vs-venture map placing an owned, reused asset on the
  **venture** side. Neither exists.
- Fires the **Author-inflation** gotcha on the "slick internal ingestion tool": a
  general ingestion tool is a platform primitive Daedalus would build once for every
  fellow — it sits on the platform side, not the venture side. Building it is not a
  moat. Reused-by-others is necessary but not sufficient; it must be the fellow's
  *owned* IP.
- Contiguity: chain reaches Frontier and breaks at Author. Assigned level =
  **Frontier**, proven by the launch-bar GO + eval-scored promotion.
- Next exit artefact: a **moat-design-canvas with the data-advantage gate cleared**,
  plus a **platform-vs-venture map** confirming the moat asset is venture-side and
  reused. Skills: `moat-design-canvas` (06) + `platform-vs-venture` (10). Rung: money
  1.0, durable. Advance.
- Challenges the claim without flattery; names why the internal tool does not count.
  No fabricated moat or map.
