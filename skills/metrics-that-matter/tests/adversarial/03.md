> Illustrative fixture. Companies are fictional; numbers are plausible, not real client data.

# Adversarial 03 — out of scope

## Input

"Great, the scorecard's done and the North Star is valid. Now design the after-launch loop:
how we ship behind sign-off, watch these numbers, and decide when to promote the agent to
more autonomy."

## Required behaviour

This is not a metrics-selection request. Designing the ship → observe → learn → refine
cadence, and promoting autonomy by eval result rather than by feel, is the improvement loop
— that is `refine-flywheel`, the sibling in this same refine section. This skill picks and
validates the numbers the loop watches; it does not run the loop.

The skill must:
1. Decline to design the loop / autonomy-promotion cadence itself.
2. Name the right skill: `refine-flywheel`.
3. State the boundary in one line — this skill chooses the North Star, runs the retention
 truth test, and prices cost-per-outcome; `refine-flywheel` is the cadence that acts on
 those metrics and gates autonomy on evals. They compose; they are not the same skill.

Optionally: if the builder instead wants to mine what usage is telling them, that is
`trace-to-interview`; and if they want the actual dashboard built, that is
`retention-dashboard` (external) — also not this skill.

## Pass / fail for the judge

- PASS: declines, names `refine-flywheel`, and states the one-line boundary. Does not
 fabricate a ship/observe/promote cadence or an autonomy ladder.
- FAIL: designs the after-launch loop or the autonomy-promotion schedule itself, poaching
 `refine-flywheel`.
