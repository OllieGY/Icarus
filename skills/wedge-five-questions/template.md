# Wedge Test — Five Questions

Fill every field. Do not average, do not round up. The verdict is `AND` across all five rows: real wedge only on 5/5.

## 1. The wedge in one sentence

> Every [recurring trigger], [who] [does one action] and gets [one output].

**Wedge:** _[write it — who, on what recurring trigger, gets what single output]_

If you cannot complete this sentence, stop. You do not yet have a wedge to test; you have an idea. Name what is missing (trigger? single output? a real user?) and go get it.

## 2. The five-row verdict

Each row needs one cited piece of evidence, tagged with its ladder score. A row passes only on evidence **≥ 0.5** (money 1.0 / behaviour 0.7 / artefact 0.5). Verbal commitment (0.3) and opinion (0.1) fail the row.

| # | Question | Hard threshold | Evidence (cite + ladder score + [Fact]/[Assumption]/[Hypothesis]) | Pass / Fail |
|---|---|---|---|---|
| 1 | Standalone value | Full job done **alone in week 1**, nothing else built | _[e.g. "Concierge-ran the output for 3 users; 2 changed behaviour on it" — behaviour 0.7 [Fact]]_ | _[PASS/FAIL]_ |
| 2 | Forced cadence | Use pulled by a **recurring event already in the user's calendar** | _[name the recurring event + evidence it pulled repeat use unprompted]_ | _[PASS/FAIL]_ |
| 3 | Travel | Output reaches **≥2 non-users/week** as normal workflow | _[who forwarded it, to how many non-users, how often — observed, not assumed]_ | _[PASS/FAIL]_ |
| 4 | Structural expansion | Using it **creates an unmet need** the wedge can't satisfy and the next build does | _[name the next build + evidence the wedge creates the need, not an upsell]_ | _[PASS/FAIL]_ |
| 5 | Scoreable adoption | Adoption = **one number you already collect** (no new instrumentation) | _[name the number + where it already lives in your data today]_ | _[PASS/FAIL]_ |

## 3. Verdict

**Score:** _[n]_ / 5

**Verdict:** _[REAL WEDGE (only if 5/5) | NOT A WEDGE — a feature in a wedge costume]_

## 4. For each failing row — the one change that flips it

| Failing # | Why it failed | Smallest change that would flip it to PASS |
|---|---|---|
| _[#]_ | _[the specific reason]_ | _[the one change — e.g. "bind it to the Friday timesheet lock instead of our reminder email"]_ |

## 5. Next step

- If **5/5**: this is the wedge. Build it, and instrument the Q5 number from day one.
- If **not 5/5**: do not build it as the wedge yet. Run the change in section 4, then re-test. Or pick a different candidate.
- Rows resting on `[Assumption]` are not passes; they are the cheapest test to run before you commit.
