> Illustrative fixture. Companies are fictional; numbers are plausible, not real client data.

# Golden 02 — Foundry Signal (industrial AI) — full pass, org-incentive veto

## Input

Wedge (5/5 on `wedge-five-questions`): "At each shift start, a factory manager opens the Foundry Signal copilot and gets a plain-language read of overnight anomalies with a suggested first action, and posts the read to the supervisors' channel."

Evidence on the table:
- 6 factory managers use it every shift across 2 plants; each accepted/rejected action is logged and fed back as a label. [Fact]
- After 8 weeks the copilot's suggested-first-action acceptance rate rose from 41% to 63% on the labelled history. [Fact]
- Two plants signed 12-month renewals after the pilot; one added a second line. [Fact]
- The corpus of anomaly→action→outcome labels is specific to each plant's equipment and is covered by the signed pilot clause. [Fact]
- The most-likely incumbent is the plants' existing MES/automation vendor, whose field engineers are compensated on billable integration hours, not on shipping self-serve AI. [Assumption]

## Expected shape of a good output

- Gate 0 confirmed 5/5.
- Four dated gates. Gate 1 cleared on behaviour 0.7 (shift-start use), Gate 3 cleared on **money 1.0** (12-month renewals + line expansion), Gate 4 supported by the plant-specific labelled corpus under a signed clause.
- Usage gate compounding deposit = the anomaly→action→outcome label corpus, which sharpens with use (41%→63% is the observable).
- Incumbent-veto sentence: MES/automation incumbent won't copy because their field-engineer org is compensated on billable integration hours — self-serve AI cannibalises their own services revenue (org-incentive + business-model-inversion row). Both tests PASS; incumbent comp structure tagged `[Assumption]`.
- Three falsifiers: usage (label corpus stops sharpening / acceptance rate flat), lock-in (renewal rate at next cycle), data advantage (red-team matches acceptance on a generic non-plant-specific model).
- Verdict: **MOAT DESIGNED.** All four gates dated; renewal money cited as the strongest gate proof.

Numbers/tokens that must appear: the 41%→63% compounding signal; the money-1.0 renewal cited at the lock-in gate; a structural (org-incentive/business-model) veto; three falsifiers.
