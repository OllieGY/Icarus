> Illustrative fixture. Companies are fictional; numbers are plausible, not real client data.

# Golden 01 — Meridian Grid (seed case) — full pass

## Input

Wedge (5/5 on `wedge-five-questions`): "Every month-end a data-centre operator uploads incident-and-uptime logs and gets a risk report ranking their sites against an anonymised peer benchmark."

Evidence on the table:
- 4 operators ran the report at two month-ends; 3 changed a maintenance-scheduling decision on it. [Fact]
- The benchmark spans those 4 operators' anonymised incident histories. [Fact]
- Pilot #1 contracts do NOT yet contain a data-rights clause. [Fact]
- Operators already run a third-party DCIM platform that promises single-tenant data isolation. [Assumption]

## Expected shape of a good output

- Gate 0 confirmed: wedge restated in one sentence, `wedge-five-questions` verdict cited as 5/5.
- Four dated gates (each with a target month, e.g. 2026-07 / 2026-10 / 2027-03 / 2027-09), entry condition, through-it signal scored on the ladder, compounding deposit.
- Gate 1 cleared on behaviour 0.7 (3 of 4 changed a decision). Gates 2–4 marked forward-looking `[Hypothesis]`/`[Assumption]`.
- Gate 4 flagged as **cannot clear yet**: data-rights clause unsigned → routed to `data-rights-clause`. Must NOT mark the corpus a moat on an unsigned claim.
- Incumbent-veto sentence naming the DCIM incumbent, blank = data-isolation contracts / channel conflict (a structural row), both tests PASS, incumbent internals tagged `[Assumption]`.
- Three monthly falsifiers, one per compounding gate (usage / lock-in / data), each with an observable threshold and a date, each able to fail.
- Verdict: **MOAT DESIGNED — with Gate 4 blocked** until the clause is signed.

Numbers/tokens that must appear: four dated gates; the veto sentence with a structural blank; exactly three falsifiers; the `data-rights-clause` routing for Gate 4.
